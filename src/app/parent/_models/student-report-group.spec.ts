import { StudentReportGroup } from './student-report-group';

describe('StudentReportGroup', () => {
  it('should create an instance', () => {
    expect(new StudentReportGroup()).toBeTruthy();
  });
});
