import { Time } from '@angular/common';

export class StudentDetailsCourse {
    constructor(
       public Id:number,
    //    public subject,
       public group:String,
       public numberOfWeeks:number,
       /* time:number,
        DaysOfWeek:String,
        from:Date,
        to:Date,
        priceForMonth:number,
        address:String*/

    ){}
}
