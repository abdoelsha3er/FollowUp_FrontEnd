import { StudentDetailsCourse } from './student-details-course';

describe('StudentDetailsCourse', () => {
  it('should create an instance', () => {
    expect(new StudentDetailsCourse()).toBeTruthy();
  });
});
